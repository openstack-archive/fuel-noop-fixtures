---
  storage:
    volume_backend_names:
      volumes_ceph: false
      volumes_lvm: 'LVM-backend'
      volumes_block_device: false
  storage_hash:
    volume_backend_names:
      volumes_ceph: false
      volumes_lvm: 'LVM-backend'
      volumes_block_device: false

